#pragma once

#define RGB_MATRIX_DEFAULT_MODE RGB_MATRIX_SOLID_COLOR
#define RGB_MATRIX_DEFAULT_HUE 135   // teal — raise toward 170 for bluer, lower toward 110 for greener
#define RGB_MATRIX_DEFAULT_SAT 255   // 255 = vivid, lower = softer/pastel
#define RGB_MATRIX_DEFAULT_VAL 150   // brightness, 0–255 

#define SPLIT_LAYER_STATE_ENABLE
